async function fetchData(ticker, timeframe) {
    try {
        const API_KEY = 'YOUR_TWELVEDATA_API_KEY';
        const response = await axios.get(
            `https://api.twelvedata.com/time_series?symbol=${ticker}&interval=5min&apikey=${API_KEY}`
        );
        // Process Twelve Data format
    } catch (error) {
        console.error('Error fetching data:', error);
        return null;
    }
}